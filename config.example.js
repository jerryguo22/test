// Copy this file to config.js and fill in your key.
// config.js is gitignored — never commit it.
//
// Only the Google Pollen raster tile key is needed at runtime.
// Census and pollen forecast data live in data/ (pre-downloaded, no runtime key needed).
// To refresh stale data run: scripts/refresh-data.sh
window.GOOGLE_POLLEN_KEY = 'YOUR_GOOGLE_POLLEN_KEY'; // https://developers.google.com/maps/documentation/pollen
